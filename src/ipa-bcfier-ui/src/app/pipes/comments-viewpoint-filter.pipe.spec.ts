import { CommentsViewpointFilterPipe } from './comments-viewpoint-filter.pipe';

describe('CommentsViewpointFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentsViewpointFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
